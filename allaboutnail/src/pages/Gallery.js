import React from 'react';
import { Typography, Box } from '@mui/material';

function Gallery() {
    return (
        <Box>
            <Typography variant="h4">Galeria prac</Typography>
            <Typography>Oto nasze najnowsze stylizacje paznokci.</Typography>
        </Box>
    );
}

export default Gallery;